# Allow Prisma and NestJS to run their build scripts
enable-pre-post-scripts=true

# Auto-approve builds for these packages
auto-install-peers=true

# Allow scripts from specific packages (Prisma)
# This ensures prisma generate runs after install
lifecycle-scripts-allowed=true

# Workspace configuration
link-workspace-packages=true
prefer-workspace-packages=true

